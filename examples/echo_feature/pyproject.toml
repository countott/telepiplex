[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "telepiplex-echo"
version = "1.0.1"
description = "Reference Telepiplex runtime Feature"
requires-python = ">=3.12"
dependencies = ["telepiplex-plugin-sdk==2.1.1"]

[tool.setuptools.packages.find]
where = ["src"]
