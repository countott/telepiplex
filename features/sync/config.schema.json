{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "x-telepiplex-config-command": "sync_config",
  "properties": {
    "category_folder": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "kind": {"enum": ["live_action_movie", "animated_movie", "live_action_series", "animated_series"]},
          "name": {"type": "string", "minLength": 1},
          "path": {"type": "string", "pattern": "^/"},
          "plex_library_id": {"type": "string"}
        },
        "required": ["kind", "name", "path", "plex_library_id"],
        "additionalProperties": false
      }
    },
    "plex": {
      "type": "object",
      "title": "Plex",
      "properties": {
        "base_url": {"type": "string"},
        "token": {"type": "string", "writeOnly": true},
        "timeout": {"type": "number", "minimum": 1, "maximum": 300},
        "scan_poll_interval": {"type": "number", "minimum": 0, "maximum": 300},
        "scan_timeout": {"type": "number", "minimum": 1, "maximum": 3600}
      },
      "required": ["base_url", "token", "timeout", "scan_poll_interval", "scan_timeout"],
      "additionalProperties": false
    },
    "tmdb": {"$ref": "#/$defs/metadata_provider", "title": "TMDB"},
    "fanart": {"$ref": "#/$defs/metadata_provider", "title": "Fanart.tv"},
    "mcp": {
      "type": "object",
      "title": "MCP",
      "properties": {
        "enabled": {"type": "boolean"},
        "host": {"type": "string"},
        "port": {"type": "integer", "minimum": 1, "maximum": 65535},
        "path": {"type": "string"},
        "auth_token": {"type": "string", "writeOnly": true}
      },
      "required": ["enabled", "host", "port", "path", "auth_token"],
      "additionalProperties": false
    }
  },
  "$defs": {
    "metadata_provider": {
      "type": "object",
      "properties": {
        "api_key": {"type": "string", "writeOnly": true},
        "timeout": {"type": "number", "minimum": 1, "maximum": 300}
      },
      "required": ["api_key", "timeout"],
      "additionalProperties": false
    }
  },
  "required": ["category_folder", "plex", "tmdb", "fanart", "mcp"],
  "additionalProperties": false
}
