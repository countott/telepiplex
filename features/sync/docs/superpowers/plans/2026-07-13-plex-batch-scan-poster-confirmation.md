# Plex 批次扫描、海报链路与写确认实施计划

1. 用失败测试证明一个多目标 `media.organized` 批次只扫描一次且逐路径定位。
2. 实现批次运行器，把共享扫描结果持久化到各 job 后复用现有逐项管线。
3. 用测试锁定普通媒体 artwork 步骤执行一次并记录选择/告警。
4. 增加只允许元数据类写操作的 MCP 批次工具与单 Token 确认。
5. 统一 `mcp.auth_token` 配置，更新 README/schema，运行全量测试、编译和配置校验。
