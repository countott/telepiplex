[build-system]
requires = ["setuptools>=75"]
build-backend = "setuptools.build_meta"

[project]
name = "telepiplex-plugin-sdk"
version = "1.1.0"
description = "Runtime SDK for Telepiplex Feature subprocesses"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]
